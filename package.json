{
  "name": "character-nudges",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "bun build src/backend.ts --outfile dist/backend.js --target bun && bun build src/frontend.ts --outfile dist/frontend.js --target browser",
    "build:backend": "bun build src/backend.ts --outfile dist/backend.js --target bun",
    "build:frontend": "bun build src/frontend.ts --outfile dist/frontend.js --target browser"
  },
  "devDependencies": {
    "lumiverse-spindle-types": "^0.2.2",
    "typescript": "^6.0.2"
  }
}
